import { RoundTwoDecimalsPipe } from './round-two-decimals.pipe';

describe('RoundTwoDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundTwoDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
