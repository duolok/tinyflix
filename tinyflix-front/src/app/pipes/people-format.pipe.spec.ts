import { PeopleFormatPipe } from './people-format.pipe';

describe('PeopleFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
